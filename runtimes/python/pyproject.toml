[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.80.0",
    "protobuf>=6.33.6",
]

[dependency-groups]
dev = [
    "grpcio-tools>=1.80.0",
]
